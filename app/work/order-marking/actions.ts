"use server";
import { revalidatePath } from "next/cache";import { redirect } from "next/navigation";import { requireAccount,requireUser } from "@/lib/auth";import { completeOrderMarkingTask } from "@/src/lib/workflow/order-route-tasks";
export async function completeOrderMarkingAction(form:FormData){const user=await requireUser();const account=await requireAccount(user);let error="";try{await completeOrderMarkingTask({taskId:String(form.get("taskId")??""),accountId:account.id,actorUserId:user.id,expectedStatus:String(form.get("expectedStatus")??""),clientRequestId:String(form.get("clientRequestId")??"")});}catch(cause){error=cause instanceof Error?cause.message:"Could not complete marking.";}revalidatePath("/work/order-marking");redirect(`/work/order-marking?${error?`error=${encodeURIComponent(error)}`:"success=1"}`);}
