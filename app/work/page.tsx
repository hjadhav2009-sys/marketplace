import Link from "next/link";
import { AppShell } from "@/components/AppShell";
import { PageHeader } from "@/components/PageHeader";
import { requireAccount,requireUser } from "@/lib/auth";
import { hasWorkPermission } from "@/lib/work-permissions";
import { getSmartStageSummary } from "@/src/lib/workflow/grouped-work";

export default async function WorkHubPage(){const user=await requireUser();const account=await requireAccount(user);const allowed=(["PICK","MARK","ASSEMBLE","PACK"] as const).filter(stage=>hasWorkPermission(user,stage==="PICK"?"canPick":stage==="MARK"?"canMark":stage==="ASSEMBLE"?"canAssemble":"canPack")||user.canViewAllWork);const summaries=await Promise.all(allowed.map(stage=>getSmartStageSummary({actorUserId:user.id,accountId:account.id,stage})));return <AppShell><PageHeader eyebrow={`${account.marketplace} / ${account.accountDisplayName??account.name}`} title="Work Hub" description="Choose a stage. Daily Orders and Consignments remain visibly separated."/><section className="grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{allowed.map((stage,index)=>{const summary=summaries[index],count=summary.ORDER.cardCount+summary.CONSIGNMENT.cardCount,quantity=summary.ORDER.requiredQuantity+summary.CONSIGNMENT.requiredQuantity;return <Link key={stage} href={`/work/${stage.toLowerCase()}`} className="rounded-md bg-slate-950 p-5 text-white"><p className="text-2xl font-black">{stage[0]}{stage.slice(1).toLowerCase()}</p><p className="mt-2 text-sm">{count} grouped cards / Qty {quantity}</p></Link>})}</section><section className="mt-5 grid gap-3 sm:grid-cols-2"><Link href="/work/scan" className="rounded-md border bg-white p-5 shadow-sm"><p className="text-xs font-black text-berry">SELECTED ACCOUNT</p><p className="mt-2 text-xl font-black">Universal Scan</p><p className="text-sm text-slate-600">Exact active work only. Scanning never mutates.</p></Link><Link href="/work/problems" className="rounded-md border bg-white p-5 shadow-sm"><p className="text-xs font-black text-rose-700">WORK EXCEPTIONS</p><p className="mt-2 text-xl font-black">Problems</p><p className="text-sm text-slate-600">Review assignment and interrupted-stage problems.</p></Link></section></AppShell>}
