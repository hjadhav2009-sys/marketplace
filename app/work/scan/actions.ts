"use server";
import { redirect } from "next/navigation";
import { requireUser } from "@/lib/auth";
import { applyUniversalCandidateAction, type UniversalCandidateAction } from "@/src/lib/workflow/universal-actions";

const value=(form:FormData,name:string,max=180)=>String(form.get(name)??"").normalize("NFKC").trim().slice(0,max);
export async function universalCandidateAction(form:FormData){const user=await requireUser();const code=value(form,"code");const intent=value(form,"intent",10)||"ANY";const accountFilter=value(form,"accountFilter",80);const returnPath=value(form,"returnPath",40)==="/packing"?"/packing":"/work/scan";let error:string|null=null;try{await applyUniversalCandidateAction({actorUserId:user.id,accountId:value(form,"accountId",80),sourceId:value(form,"sourceId",80),action:value(form,"action",30) as UniversalCandidateAction,expectedQuantity:Number(value(form,"expectedQuantity",20)),expectedStatus:value(form,"expectedStatus",30),clientRequestId:value(form,"clientRequestId",160),manualTitle:value(form,"manualTitle",160),manualInstructions:value(form,"manualInstructions",2000),manualImageUrl:value(form,"manualImageUrl",2048)});}catch(cause){error=cause instanceof Error?cause.message:"Action failed.";}const params=new URLSearchParams({intent});if(accountFilter)params.set("accountId",accountFilter);if(error)params.set("q",code);params.set(error?"scanError":"scanSuccess",error??"Action completed. Scan the next item.");redirect(`${returnPath}?${params}`);}
