# Phase 7.3.6 Stage 4.3A Before/After Index

The existing private synthetic captures remain under `.codex-tmp` and are not
committed.

Verified before this audit:

- default, invalid and expired login states at six widths;
- responsive Pick/App Shell references at six widths;
- account-menu-open references at mobile widths;
- corrected tablet and desktop Pick-card masters.

Pending recapture after interactive browser access is restored:

- authentication states after password and banner accessibility changes;
- account-menu outside-click and Escape states;
- successful synthetic login;
- one genuine forbidden/access-denied state;
- role-specific drawer navigation;
- Pick action interactions.

No existing private capture was deleted or relabelled as a genuine forbidden
state.
