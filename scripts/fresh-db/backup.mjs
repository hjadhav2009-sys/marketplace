import { createBackup, requireOwnerArgument, resolveRealDatabasePath } from "./core.mjs";
try { const result=await createBackup(resolveRealDatabasePath(),requireOwnerArgument()); console.log(JSON.stringify({created:true,manifestPath:result.manifestPath,backupPath:result.manifest.backupPath,backupSha256:result.manifest.backupSha256,integrity:result.manifest.integrity,foreignKeyViolationCount:result.manifest.foreignKeyViolationCount,storage:result.manifest.activeStorage},null,2)); } catch(error){ console.error(error instanceof Error?error.message:String(error)); process.exit(1); }
