import assert from "node:assert/strict";
import { execFileSync } from "node:child_process";
import { DatabaseSync } from "node:sqlite";
import { mkdirSync, readFileSync, readdirSync, rmSync, writeFileSync } from "node:fs";
import { join, resolve } from "node:path";

const root=resolve(process.cwd(),".codex-tmp");mkdirSync(root,{recursive:true});const file=resolve(root,"operational-retention.db");rmSync(file,{force:true});const sqlite=new DatabaseSync(file);sqlite.exec("PRAGMA foreign_keys=ON;");for(const name of readdirSync(resolve("prisma/migrations"),{withFileTypes:true}).filter(item=>item.isDirectory()).map(item=>item.name).sort())sqlite.exec(readFileSync(join("prisma/migrations",name,"migration.sql"),"utf8"));sqlite.exec("INSERT INTO Account (id,name,code,marketplace,active,createdAt,updatedAt) VALUES ('account','Synthetic','SYN','FLIPKART',1,CURRENT_TIMESTAMP,CURRENT_TIMESTAMP)");sqlite.exec("INSERT INTO WorkChangeEvent (accountId,eventType,sourceType,createdAt) VALUES ('account','SYNTHETIC','ORDER',946684800000)");sqlite.close();
const command=[resolve("node_modules/tsx/dist/cli.mjs"),resolve("scripts/operational-retention.ts")],env={...process.env,DATABASE_URL:`file:${file.replace(/\\/g,"/")}`};const output=execFileSync(process.execPath,command,{cwd:process.cwd(),env,encoding:"utf8"});assert.match(output,/"dryRun": true/);let verify=new DatabaseSync(file,{readOnly:true});assert.equal((verify.prepare("SELECT COUNT(*) AS count FROM WorkChangeEvent").get() as {count:number}).count,1,"Retention dry-run never deletes eligible records");verify.close();assert.throws(()=>execFileSync(process.execPath,[...command,"--confirm-cleanup"],{cwd:process.cwd(),env,encoding:"utf8",stdio:"pipe"}),/status 1|Command failed/);const backup=resolve(root,"verified-backup-manifest.json");writeFileSync(backup,JSON.stringify({verified:true,completedAt:new Date().toISOString(),databaseSha256:"a".repeat(64)}));const confirmed=execFileSync(process.execPath,[...command,"--confirm-cleanup",`--backup-manifest=${backup}`],{cwd:process.cwd(),env,encoding:"utf8"});assert.match(confirmed,/"dryRun": false/);verify=new DatabaseSync(file,{readOnly:true});assert.equal((verify.prepare("SELECT COUNT(*) AS count FROM WorkChangeEvent").get() as {count:number}).count,0,"Verified confirmed cleanup removes eligible history");verify.close();rmSync(backup,{force:true});rmSync(file,{force:true});console.log("Operational retention dry-run, backup proof and confirmed cleanup tests passed.");
