import assert from "node:assert/strict";
import { createTempWorkflowDb } from "./temp-workflow-db";
import { getGroupedWork } from "../src/lib/workflow/grouped-work";
import { setGroupedProgress } from "../src/lib/workflow/grouped-progress";
const {db,cleanup}=createTempWorkflowDb("partial-progress-authorization");
try{await db.account.createMany({data:[{id:"a",name:"A",code:"A",marketplace:"FLIPKART"},{id:"b",name:"B",code:"B",marketplace:"FLIPKART"}]});await db.user.create({data:{id:"w",username:"auth-worker",passwordHash:"x",name:"Worker",role:"PICKER",accountId:"a",canPick:true}});await db.uploadBatch.create({data:{id:"batch",accountId:"a",fileName:"x"}});for(let index=0;index<2;index++){await db.order.create({data:{id:"o"+index,accountId:"a",batchId:"batch",marketplace:"FLIPKART",awb:"A"+index,sku:"SKU",qty:1,orderNo:"O"+index}});await db.workTask.create({data:{accountId:"a",sourceType:"ORDER",orderId:"o"+index,stage:"PICK",sequenceNumber:1,requiredQuantity:1,status:"READY",workCardSnapshotJson:JSON.stringify({sellerSku:"SKU"})}});}const card=(await getGroupedWork({actorUserId:"w",accountId:"a",stage:"PICK",sourceType:"ORDER"},db)).cards[0],request={actorUserId:"w",selectedAccountId:"a",sourceType:"ORDER" as const,stage:"PICK" as const,groupKey:card.groupKey,expectedGroupVersion:card.groupVersion,targetCompletedQuantity:1,clientRequestId:"known-request"};await setGroupedProgress(request,db);await db.user.update({where:{id:"w"},data:{accountId:"b"}});await assert.rejects(()=>setGroupedProgress(request,db),/not assigned/);}finally{await cleanup();}console.log("Partial progress authorization-before-replay tests passed.");
